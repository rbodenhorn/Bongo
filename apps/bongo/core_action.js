sc_require('core');

SC.mixin(Bongo, {
  addFolder: function (sender){
    alert("add folder called");
      }
});

SC.mixin(Bongo, {
  removeFolder: function (sender){
    alert("remove folder called")
  }
});